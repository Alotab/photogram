import React from 'react'

interface IUserListProps {}

const UserList: React.FunctionComponent<IUserListProps> = (props: Props) => {
  return (
    <div>UserList</div>
  )
}

export default UserList;